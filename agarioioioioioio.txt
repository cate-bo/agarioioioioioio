agarioioioioioio

server:
-node 
-update loop (30hz)
-position
-spawns 

client:
-menu (name, leaderboard )
-update loop (30hz)
-input (mouse pos, space)
-rendering


server client communication:
-websocket


messages ( cts = client to server, stc = server to client):
'play'
-cts: name of player (send to start playing but don't initialise game yet)
-stc: circle object of player (client sees game after receiving this)
'died'
-stc: empty
'game_update'
-stc: arrays of spawned dots, eaten dots, all circles
'direction'
-cts: current direction